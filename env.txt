# Environment variables for Resume Reviewer
# Replace with your real keys before running in production.

APP_HOST=0.0.0.0
APP_PORT=8000

# Future parts (used in Parts 2-3)
GROQ_API_KEY=changeme
HF_API_KEY=changeme

